apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus        # pinned so dashboards can reference by stable uid
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
      httpMethod: POST
